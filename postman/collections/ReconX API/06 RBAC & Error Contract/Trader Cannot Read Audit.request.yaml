$kind: http-request
url: "{{baseUrl}}/v1/audit/trades/{{seedTradeRef}}"
method: GET
auth:
  type: bearer
  credentials:
    token: "{{traderToken}}"
scripts:
  - type: afterResponse
    code: pm.test("status is 403", () => pm.response.to.have.status(403));
    language: text/javascript
order: 3000
