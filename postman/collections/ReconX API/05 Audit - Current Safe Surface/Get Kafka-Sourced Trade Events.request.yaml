$kind: http-request
description: ADV138 placeholder. Do not treat an empty list as the permanent contract.
url: "{{baseUrl}}/v1/audit/trades/{{seedTradeRef}}/events"
method: GET
headers:
  - key: Accept
    value: application/json
scripts:
  - type: afterResponse
    code: |-
      pm.test("status is 200", () => pm.response.to.have.status(200));
      pm.test("event history is a list", () => pm.expect(pm.response.json()).to.be.an("array"));
    language: text/javascript
order: 2000
